#include "main.h"

/**
 * _strlen - Retruns the length of a string.
 * @str: a string pointer.
 *
 *
 * Return: 1.
 */

int _strlen(char *str)
{
	int i;
	
	for (i = 0; str[i] != 0; i++)
	{
		;
	}
	return (i);
}
